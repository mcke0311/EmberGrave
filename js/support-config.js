// Paste the creator's public HTTPS donation / crowdfunding page URL here.
// Keep this empty until the real payment destination is ready. Never add secrets.
window.EMBERGRAVE_SUPPORT = Object.freeze({ donationUrl: 'https://ko-fi.com/embergrave' });
